options(HTTPUserAgent = sprintf("R/%s R (%s)", getRversion(), paste(getRversion(), R.version["platform"], R.version["arch"], R.version["os"])))
# options(repos = c(PPM = "https://packagemanager.posit.co/cran/__linux__/centos7/latest"))
options(repos = c(PPM = "https://packagemanager.posit.co/cran/__linux__/jammy/latest"))

# options(repos = c(RSPM = "https://packagemanager.rstudio.com/all/__linux__/centos7/latest"))
# options(repos = c(CRAN="https://cran.rstudio.com" ))

source("renv/activate.R")
